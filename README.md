# shotlix

---

## How to run

```:sh
# clone
$ git clone git@github.com:shotlix/shotlix.git
$ cd shotlix

# install package
$ yarn install

# listen server (PORT: 8000)
$ yarn start

# compile sass
$ yarn scss

# add prefix
$ yarn prefix

# open hotreload develop server
$ yarn hot-dev-server
```
